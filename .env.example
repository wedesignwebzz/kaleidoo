EMAIL_PROVIDER=resend           # or "gmail"
NOTIFY_EMAIL=kaleidoojobsearch@gmail.com

# Resend (default — recommended)
RESEND_API_KEY=
RESEND_AUDIENCE_ID=
FROM_EMAIL=waitlist@kaleidoo.co.uk   # use onboarding@resend.dev until domain is verified

# Gmail SMTP (alternative — requires 2FA + Gmail App Password, NOT your normal password)
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Optional Postgres/MySQL backup (skipped silently if absent)
DATABASE_URL=
